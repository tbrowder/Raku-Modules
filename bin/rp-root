#!/usr/bin/env raku

use lib <../lib>;
use RakudoPkgHelper;

my $user = $*USER;
note "DEBUG user = $user";

if not @*ARGS {
    say qq:to/HERE/;
    Usage: {$*PROGRAM.basename} go

    HERE
    exit
}

