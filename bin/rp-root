#!/usr/bin/env raku

use lib <../lib>;
use RakudoPkgHelper;

my $user    = $*USER;
my $is-root = $user eq 'root' ?? True !! False;

if not @*ARGS {
    say qq:to/HERE/;
    Usage: {$*PROGRAM.basename} go

    HERE
    exit
}

